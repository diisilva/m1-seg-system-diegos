# Porta de host para acessar o Juice Shop (127.0.0.1:<porta>).
# Copie este arquivo para .env caso a porta 3000 ja esteja em uso na sua
# maquina; nao e necessario editar o docker-compose.yml.
JUICESHOP_HTTP_PORT=3000
