# Porta de host para acessar o Juice Shop (127.0.0.1:<porta>).
# Copie este arquivo para .env nesta mesma pasta caso a porta padrao esteja
# ocupada por outro processo nesta maquina; nao e necessario editar o
# docker-compose.yml.
JUICESHOP_HTTP_PORT=3000
