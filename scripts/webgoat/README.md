# scripts/webgoat

Provas de conceito específicas do cenário OWASP WebGoat. Um módulo por categoria OWASP investigada (ex.: `a03_injection.py`), usando `scripts/common` para sessão HTTP, gravação de evidência e trava de escopo.

_Scripts a criar durante a investigação deste cenário._
